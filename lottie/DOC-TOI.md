# Sticker Lottie
Bỏ tệp .json tải từ LottieFiles (chọn loại giấy phép miễn phí) vào đây.
Đạo diễn sẽ thấy danh sách tệp và được phép chèn bằng module `lottie`.
Chưa có tệp thì đã có sẵn 3 sticker vẽ tay: mũi tên (`mui-ten`), pháo giấy (`confetti`), lấp lánh (`lap-lanh`) qua module `sticker`.
